import React from "react";
import { Text, View } from "react-native";

const MenuScreen = React.memo((props: any) => {
    return (
        <View>
            <Text>Hello</Text>
        </View>
    )
})

export default MenuScreen